/**
 * Generated by orval v8.21.0 🍺
 * Do not edit manually.
 * Api
 * API specification
 * OpenAPI spec version: 0.1.0
 */
export interface HealthStatus {
  status: string;
}

export interface ErrorResponse {
  error: string;
}

export interface TeraboxCookieStatus {
  valid: boolean;
  reason: string;
}

/**
 * A file or folder within a resolved share.
 */
export interface TeraboxNode {
  name: string;
  path: string;
  isDir: boolean;
  size: number;
  fsId: string;
  /** @nullable */
  children?: TeraboxNode[] | null;
}

export interface TeraboxResolveInput {
  /** A TeraBox share URL, e.g. https://terasharefile.com/s/xxxx */
  url: string;
}

export interface TeraboxShare {
  title: string;
  fileCount: number;
  totalSize: number;
  tree: TeraboxNode[];
}

export interface TeraboxDownloadInput {
  /** The original share URL passed to resolve. */
  url: string;
  /** @minItems 1 */
  fsIds: string[];
  /** Suggested filename for the download. */
  filename?: string;
}

export interface TeraboxDownloadLink {
  /** Signed TeraBox CDN URL to download the file(s) directly. The frontend should navigate/link to this URL as-is (it is not same-origin) so the browser performs the fetch from the user's own IP. */
  downloadUrl: string;
  isZip: boolean;
}

/**
 * A single file within a resolved share, with its direct download URL already resolved.
 */
export interface TeraboxDlinkFile {
  name: string;
  /** Folder path within the share, e.g. "/post 357/bonus videos". */
  path: string;
  size: number;
  fsId: string;
  /** Signed TeraBox CDN URL. Use as-is; do not proxy the bytes through this server. */
  downloadLink: string;
}

export interface TeraboxDlinksResponse {
  title: string;
  fileCount: number;
  totalSize: number;
  files: TeraboxDlinkFile[];
}

